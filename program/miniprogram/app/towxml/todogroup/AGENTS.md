# `towxml/todogroup/` TodoList 组件（Towxml）

用于渲染 Markdown 的 todo 语法（checkbox 列表）。

## 文件说明

- `todogroup.json`：声明组件。
- `todogroup.wxml` / `todogroup.wxss`：checkbox 组结构与样式。
- `todogroup.js`：组件逻辑（change 事件等）。

