# `towxml/parse/parse2/entities/maps/` 实体映射表（Towxml）

该目录为 entities 的映射数据表（上游生成/维护），供 decode/encode 使用。

## 文件说明

- `entities.js`：通用实体映射。
- `legacy.js`：legacy 实体映射。
- `xml.js`：XML 实体映射。
- `decode.js`：decode 相关映射数据。

