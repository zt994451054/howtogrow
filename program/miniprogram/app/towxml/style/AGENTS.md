# `towxml/style/` Towxml 样式

Towxml 渲染出来的节点会带有特定 class（如 `h2w__*`）。本目录提供基础样式与主题样式。

## 文件说明

- `main.wxss`：Towxml 默认正文样式（标题、段落、列表、代码块等）。
- `theme/`：主题样式（light/dark）。

## 使用方式

- `towxml/towxml.wxss` 会引入这些样式（视上游实现）。

