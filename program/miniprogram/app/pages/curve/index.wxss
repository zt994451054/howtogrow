.page {
  min-height: 100vh;
  height: 100vh;
  /* background: var(--bg-page); */
  background-color: #fff;
  display: flex;
  flex-direction: column;
}

.nav {
  position: sticky;
  top: 0;
  z-index: 30;
  background: #fff;
}

.status {
  background: #fff;
  height: var(--page-nav-height);
}

.header {
  padding: var(--page-padding) var(--page-padding) 16rpx;
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(10px);
}

.loading {
  padding: 60rpx 0;
  text-align: center;
  color: var(--text-muted);
  font-size: 24rpx;
  font-weight: 700;
}

.a {
  align-items: center;
  gap: 16rpx;
}

.avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 9999rpx;
  background: var(--brand);
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.avatar__img {
  width: 100%;
  height: 100%;
}

.avatar__text {
  font-size: 24rpx;
  font-weight: 800;
}

.greet {
  font-size: 30rpx;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.2;
}

.icon-btn {
  width: 72rpx;
  height: 72rpx;
  border-radius: 9999rpx;
  background: #fff;
  /* box-shadow: var(--shadow-sm); */
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  justify-content: center;
}

.menu-layer {
  position: fixed;
  inset: 0;
  z-index: 999;
}

.menu-mask {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.18);
  opacity: 1;
}

.menu-pop {
  position: absolute;
  right: 33rpx;
  width: 320rpx;
  background: #fff;
  border-radius: var(--radius-16);
  /* border: 2rpx solid rgba(17, 24, 39, 0.06); */
  overflow: hidden;
  animation: fadeIn 0.18s ease-out forwards;
}

.menu-pop__title {
  font-size: 20rpx;
  color: var(--text-muted);
  padding: 20rpx 20rpx 20rpx 26rpx;
}

.menu-pop__list {
  padding: 0 8rpx 10rpx;
}

.menu-item {
  width: 270rpx;
  text-align: left;
  background: transparent;
  border-radius: 14rpx;
  padding: 18rpx 16rpx;
  color: var(--text-secondary);
  font-size: 26rpx;
  font-weight: 700;
}

.menu-item::after {
  border: none;
}

.menu-item.on {
  background: rgba(249, 115, 22, 0.10);
  color: var(--brand);
}

.menu-item__row {
  align-items: center;
}

.menu-item__name {
  max-width: 240rpx;
}

.menu-item__dot {
  width: 12rpx;
  height: 12rpx;
  border-radius: 9999rpx;
  background: var(--brand);
}

.menu-item.add {
  color: var(--brand);
  font-weight: 600;
  padding: 18rpx 0rpx;
}

.body {
  /* background: var(--bg-page); */
  background-color: #fff;
  flex: 1;
  min-height: 0;
}

.wrap {
  padding: var(--section-gap) var(--page-padding) 260rpx;
}

.empty {
  padding: 120rpx var(--page-padding) 220rpx;
}

.empty__logo {
  width: 160rpx;
  height: 160rpx;
  border-radius: 9999rpx;
  background: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 28rpx;
}

.logo-dot {
  width: 72rpx;
  height: 72rpx;
  border-radius: 9999rpx;
  background: var(--brand);
  position: relative;
}

.logo-smile {
  width: 34rpx;
  height: 18rpx;
  border: 6rpx solid #fff;
  border-top: 0;
  border-left: 0;
  border-right: 0;
  border-radius: 0 0 9999rpx 9999rpx;
  position: absolute;
  left: 18rpx;
  top: 26rpx;
}

.logo-tail {
  width: 18rpx;
  height: 28rpx;
  background: rgba(255, 255, 255, 0.7);
  border-radius: 9999rpx;
  position: absolute;
  right: -10rpx;
  bottom: 8rpx;
  transform: rotate(10deg);
}

.empty__title {
  font-size: 34rpx;
  font-weight: 800;
  color: var(--text-primary);
  margin-bottom: 10rpx;
}

.empty__desc {
  font-size: 24rpx;
  color: var(--text-muted);
  text-align: center;
  white-space: pre-line;
  line-height: 40rpx;
  margin-bottom: 40rpx;
}

.btn-primary {
  background: var(--brand);
  color: #fff;
  padding: 24rpx 64rpx;
  border-radius: var(--radius-pill);
  font-size: 28rpx;
  font-weight: 800;
}

.btn-primary::after {
  border: none;
}

.section-head {
  margin-top: 10rpx;
  margin-bottom: 18rpx;
  align-items: center;
  font-size: 26.38rpx;
  font-weight: 800;
  line-height: 26rpx;
}

.section-head__title {
  min-width: 107rpx;
  height: 26rpx;
  font-size: inherit;
  font-weight: inherit;
  text-align: left;
  color: var(--brand);
  line-height: inherit;
  white-space: nowrap;
}

.date-chip {
  padding: 14rpx 0;
  border-radius: var(--radius-pill);
  background: #fff;
  /* border: 2rpx solid rgba(17, 24, 39, 0.06); */
  /* box-shadow: var(--shadow-sm); */
  color: var(--text-secondary);
}

.date-chip__text {
  height: 26rpx;
  font-size: inherit;
  font-weight: inherit;
  text-align: left;
  color: var(--text-primary);
  line-height: inherit;
  white-space: nowrap;
}

.section {
  align-items: center;
  margin-top: 100rpx;
}

.section__title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--brand);
  line-height: 1;
}

.section__hint {
  font-size: 20rpx;
  color: var(--text-muted);
}

.chart-card {
  margin-top: var(--section-gap);
  background: #fff;
  border-radius: 32rpx;
  padding: 40rpx 12rpx;
  position: relative;
  overflow: hidden;
}

.dim-pills {
  margin-top: var(--section-gap);
}

.dim-panel {
  background: #fff;
  border-radius: 32rpx;
  padding: 20rpx 24rpx;
  box-shadow: var(--shadow-sm);
}

.dim-item {
  display: flex;
  align-items: flex-start;
  gap: 18rpx;
  padding: 14rpx 0;
}

.dim-item + .dim-item {
  padding-top: 18rpx;
}

.dim-item__dot {
  width: 32rpx;
  height: 32rpx;
  border-radius: 10rpx;
  flex-shrink: 0;
  margin-top: 6rpx;
  box-shadow: 0 2rpx 6rpx rgba(15, 23, 42, 0.08);
}

.dim-item__content {
  flex: 1;
  min-width: 0;
}

.dim-item__title {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--text-ink);
  line-height: 1.3;
}

.dim-item__desc {
  margin-top: 8rpx;
  font-size: 24rpx;
  color: var(--text-muted);
  line-height: 1.5;
}

.persist {
  margin-top: var(--section-gap);
  text-align: center;
}

.persist__icon {
  width: 104rpx;
  height: 104rpx;
  border-radius: 9999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  margin-top: 78rpx;
  margin-bottom: 34rpx;
}


.persist__name {
  margin-top: 18rpx;
  font-size: 20rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.persist__desc {
  margin-top: 10rpx;
  font-size: 26rpx;
  font-weight: 700;
  color: var(--text-secondary);
}

.persist__num {
  color: var(--brand);
  font-weight: 600;
}

.persist__hint {
  margin-top: 10rpx;
  padding: 0 48rpx;
  font-size: 22rpx;
  color: var(--text-muted);
  line-height: 1.6;
}

.mood-card {
  margin-top: var(--section-gap);
}

.mood-card__panel {
  background: #fff7f0;
  border-radius: 32rpx;
  padding: 36rpx 24rpx 40rpx;
}

.mood-card__head {
  align-items: center;
  margin-top: 100rpx;
  margin-bottom: 60rpx;
}

.mood-card__title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--brand);
  line-height: 1;
}

.mood-card__sub {
  font-size: 22rpx;
  font-weight: 700;
  color: var(--text-muted);
  line-height: 1;
}

.mood-row {
  /* margin-top: 32rpx; */
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 24rpx 12rpx;
}

.mood-row__item {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.mood-row__face {
  width: 96rpx;
  height: 96rpx;
  border-radius: 9999rpx;
  overflow: hidden;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

.mood-row__img {
  position: absolute;
  left: 50%;
  top: 50%;
  width: calc(100% + 2rpx);
  height: calc(100% + 2rpx);
  transform: translate(-50%, -50%);
  border-radius: 9999rpx;
  display: block;
}

.mood-row__emoji {
  font-size: 44rpx;
  line-height: 1;
}

.mood-row__label {
  margin-top: 10rpx;
  font-size: 22rpx;
  font-weight: 600;
  color: var(--text-muted);
  line-height: 1.2;
  text-align: center;
  white-space: nowrap;
}

.mood-row__pill {
  margin-top: 10rpx;
  width: 96rpx;
  height: 32rpx;
  border-radius: 9999rpx;
  background: #e7e7e7;
  display: flex;
  align-items: center;
  justify-content: center;
}

.mood-row__pct {
  font-size: 22rpx;
  font-weight: 600;
  color: var(--text-muted);
  line-height: 1;
}

.mood-row__pct.on {
  color: var(--pct-color, var(--brand));
}

.mood-stack {
  margin-top: 34rpx;
}

.mood-stack__bar {
  height: 40rpx;
  border-radius: 9999rpx;
  overflow: hidden;
  display: flex;
  background: rgba(17, 24, 39, 0.08);
}

.mood-stack__bar.empty {
  background: rgba(17, 24, 39, 0.06);
}

.mood-stack__seg {
  height: 100%;
}

.mood-stack__seg.first {
  border-radius: 9999rpx 0 0 9999rpx;
}

.mood-stack__seg.first.last {
  border-radius: 9999rpx;
}

.mood-stack__seg.last {
  border-radius: 0 9999rpx 9999rpx 0;
}

.chart {
  display: block;
  width: 100%;
  height: 520rpx;
}

.chart-empty {
  position: absolute;
  inset: 0;
  padding: 0 48rpx;
  text-align: center;
  background: transparent;
}

.chart-empty__t1 {
  font-weight: 600;
  color: var(--text-primary);
  font-size: 28rpx;
}

.chart-empty__t2 {
  margin-top: 10rpx;
  color: var(--text-muted);
  font-size: 22rpx;
  line-height: 32rpx;
}

.chart-empty__btn {
  margin-top: 22rpx;
  width: 320rpx;
  border-radius: var(--radius-pill);
  padding: 20rpx 0;
  background: var(--brand);
  color: #fff;
  font-weight: 600;
  font-size: 24rpx;
}

.chart-empty__btn::after {
  border: none;
}

.stats {
  margin-top: var(--section-gap);
}

.stats__inner {
  background: #fff;
  border-radius: 24rpx;
  border: 2rpx solid rgba(17, 24, 39, 0.06);
  overflow: hidden;
}

.stat {
  flex: 1;
  padding: 16rpx 18rpx;
}

.stats__divider {
  width: 2rpx;
  background: rgba(243, 244, 246, 1);
}

.stat__k {
  font-size: 20rpx;
  color: var(--text-muted);
  font-weight: 800;
}

.stat__v {
  margin-top: 6rpx;
  font-size: 24rpx;
  color: var(--text-primary);
  font-weight: 600;
}

.moods {
  margin-top: 12rpx;
  white-space: nowrap;
}

.mood {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-end;
  width: 92rpx;
  margin-right: 16rpx;
}

.mood__pct {
  font-size: 18rpx;
  font-weight: 600;
  color: var(--text-muted);
  margin-bottom: 8rpx;
}

.mood__bar {
  width: 14rpx;
  border-radius: 9999rpx;
  margin-bottom: 10rpx;
}

.mood__face {
  width: 64rpx;
  height: 64rpx;
  border-radius: 9999rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 34rpx;
}

.mood__name {
  margin-top: 10rpx;
  font-size: 20rpx;
  color: var(--text-secondary);
  font-weight: 800;
}

.more {
  align-items: center;
  gap: 8rpx;
}

.more__text {
  font-size: 22rpx;
  color: var(--text-muted);
  font-weight: 800;
  line-height: 1;
}

.more__icon {
  font-size: 26rpx;
  color: var(--text-muted);
  line-height: 1;
}

.troubles {
  margin-top: var(--section-gap);
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 16rpx;
}

.trouble-empty {
  margin-top: var(--section-gap);
  padding: 24rpx 0;
  text-align: center;
  color: var(--text-muted);
  font-size: 24rpx;
  font-weight: 700;
}

.trouble {
  background: #fff;
  border-radius: 24rpx;
  border: 2rpx solid rgba(17, 24, 39, 0.06);
  padding: 18rpx 12rpx;
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 10rpx;
  min-height: 200rpx;
}

.trouble__rank {
  position: absolute;
  left: 14rpx;
  top: 10rpx;
  font-size: 20rpx;
  font-weight: 600;
  color: rgba(156, 163, 175, 0.9);
}

.trouble__icon {
  width: 108rpx;
  height: 108rpx;
  border-radius: 9999rpx;
  background: #fff;
  border: 2rpx solid rgba(17, 24, 39, 0.06);
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}

.trouble__img {
  width: 100%;
  height: 100%;
}

.trouble__fallback {
  font-size: 28rpx;
  font-weight: 600;
  color: rgba(156, 163, 175, 1);
}

.trouble__name {
  font-size: 20rpx;
  color: rgba(60, 60, 59, 0.5);
  text-align: center;
  font-weight: bold;
}

.footer-hint {
  margin-top: var(--section-gap);
  text-align: center;
  font-size: 24rpx;
  color: #F0811D;
  line-height: 38rpx;
  font-weight: bold;
  
}
.footer-hint .def {
  color: #000000;
  margin-right: 10rpx;
}

.sheet {
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  z-index: 2000;
  visibility: hidden;
  opacity: 0;
  transition: all 0.22s;
}

.sheet.show {
  visibility: visible;
  opacity: 1;
}

.sheet__mask {
  position: absolute;
  inset: 0;
  background: rgba(17, 24, 39, 0.45);
}

.sheet__panel {
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  background: #fff;
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.22s;
  /* Leave extra room for the custom tabbar + floating center button on iOS. */
  padding: 28rpx;
  padding-bottom: calc(34rpx + 180rpx + constant(safe-area-inset-bottom));
  padding-bottom: calc(34rpx + 180rpx + env(safe-area-inset-bottom));
}

.sheet.show .sheet__panel {
  transform: translateY(0);
}

.sheet__head {
  align-items: center;
}

.sheet__title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.sheet__close {
  font-size: 44rpx;
  line-height: 1;
  color: var(--text-muted);
  padding: 6rpx 10rpx;
}

.sheet__sub {
  margin-top: 10rpx;
  color: var(--text-muted);
  font-size: 22rpx;
  font-weight: 700;
}

.range-form {
  margin-top: 18rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.range-field__label {
  font-size: 20rpx;
  color: var(--text-muted);
  font-weight: 700;
  margin-bottom: 8rpx;
}

.range-field__value {
  background: rgba(243, 244, 246, 0.9);
  border-radius: 18rpx;
  padding: 22rpx 20rpx;
  font-size: 26rpx;
  font-weight: 800;
  color: var(--text-primary);
  align-items: center;
}

.range-field__arrow {
  color: rgba(156, 163, 175, 1);
  font-size: 30rpx;
  transform: rotate(90deg);
}

.range-hint {
  margin-top: 14rpx;
  font-size: 22rpx;
  color: var(--text-muted);
  font-weight: 700;
  text-align: center;
}

.range-actions {
  margin-top: 22rpx;
  gap: 18rpx;
}

.range-btn {
  flex: 1 1 0;
  border-radius: var(--radius-pill);
  padding: 22rpx 0;
  font-size: 28rpx;
  font-weight: 600;
}

.range-btn::after {
  border: none;
}

.range-btn.ghost {
  background: rgba(243, 244, 246, 1);
  color: var(--text-secondary);
}

.range-btn.primary {
  background: var(--brand);
  color: #fff;
}
