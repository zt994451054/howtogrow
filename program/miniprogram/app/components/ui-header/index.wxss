.wrap {
  position: sticky;
  top: 0;
  background: rgba(255, 255, 255, 0.96);
  backdrop-filter: blur(10px);
  z-index: 20;
}

.bar {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 0 24rpx;
}

.side {
  width: 72rpx;
  display: flex;
  align-items: center;
  justify-content: flex-start;
}

.side.right {
  justify-content: flex-end;
}

.btn {
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  /* color: var(--text-secondary);
  background: #fff;
  border-radius: 9999rpx;
  box-shadow: var(--shadow-sm);
  border: 2rpx solid rgba(17, 24, 39, 0.06); */
}

.btn:active {
  transform: scale(0.96);
}

.center-content {
  flex: 1;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100%;
}

.logo-image {
  height: 48rpx; /* Adjust based on design requirement, keeping it within bar height */
  width: auto;
}

.title {
  font-weight: bold;
  font-size: 25.37rpx;
  text-align: left;
  color: #000000 100%;
}

.left-icon {
  width: 48rpx;
  height: 48rpx;
}
